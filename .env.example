# eCFR API Configuration
API_AUTH_TOKEN=changeme-local

# AWS Configuration (optional - uses AWS CLI default if not set)
AWS_REGION=us-east-1

# Terraform Variables (optional)
TF_VAR_api_auth_token=changeme-local